/* Loads the result data, which should be the upload's URL. */
//results = async_load[? "result"];

/* Sets clipboard to the uploaded path */
//clipboard_set_text(global.url + results);

/* Sets our message for display */
//show_message_time = 200;
//msg = "URL Copied to Clipboard";